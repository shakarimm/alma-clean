import { createStore } from 'vuex';
import Location from '@/store/modules/Location';

export default createStore({
  state: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    location: Location,
  },
});
